import React from "react";

const Footer = ({footer}) => {
    return (
        <footer className="bg-warning p-4">
            <p className="text-center mb-0">{footer}</p>
        </footer>
    )
}

export default Footer